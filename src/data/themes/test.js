import defaultTheme from "./default";

const test = {
  ...defaultTheme,
  primaryColor: "#ffc0cb",
  backgroundColor: "#800080",
  cautionColor: "#ffff31",
  hazardColor: "#a80064",
};

export default test;
